#include <stdio.h>

/**
  * main - Prints some letters of alphabet
  *
  * Return: Always (Success)
  */

int main(void)
{
int c;

for (c = '0'; c >= '9'; c++)
{
putchar(c);
}
putchar('\n');

return (0);
}
